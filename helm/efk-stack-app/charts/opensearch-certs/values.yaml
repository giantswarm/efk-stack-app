# Default values for opensearch-certs.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

enabled: true

image:
  registry: quay.io
  repository: giantswarm/opensearch-certs
  tag: v0.0.3

organization: Giant Swarm
expiration: "5 years"
